# Ejemplo de uso _**useForm**_

<br>

```
const [values, handleInputChange] = useForm(initialState={});
```

_useForm_ Retorna una función del `input[type="text"]` llamado _handleInputChange_. Además el valor que contenga el `input`.


